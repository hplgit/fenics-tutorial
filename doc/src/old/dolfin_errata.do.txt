# Various errors found in the DOLFIN documntation.

* doc string in
   `solve` function refers to `list_krylov_methods()` and similar, but
   these have changed names (to, e.g., `list_krylov_solver_methods`).
